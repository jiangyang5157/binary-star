# Singularity Code Review — Full System Audit

**Date:** 2026-06-24  
**Branch:** v26.6.24  
**Scope:** 65 Python source files, ~12,000 LOC + 4 prompt templates + 3 config YAML files  
**Reviewers:** Claude Code (DeepSeek V4 Pro) + 6 parallel subagent reviewers covering:
- Orchestration & Agents | Market Analyzer | Infrastructure & AI | Config, Utils & Math | Dashboard, API & Sniper | Entry Points & Tests

**Methodology:** Full manual deep-dive of all production code + 6 parallel automated subsystem reviews, each reading every file completely.

**Test Suite:** All 150 tests pass in 5.89 seconds.

---

## Executive Summary

Singularity is a **well-architected, production-grade crypto trading engine** with a genuinely innovative adversarial LLM debate protocol (Binary Star). The code quality is **above average** — strong use of dependency injection, frozen dataclasses, abstract interfaces, and domain-driven error hierarchies.

**However**, the system has **12 CRITICAL bugs** (4 are hard runtime crashes), plus substantial **HIGH-severity issues** in market data calculations, order execution race conditions, and prompt→code logic divergence.

### Overall Risk Score: **5.9/10** (downgraded from 7.2 → 6.8 → 5.9 with additional findings)
(10 = perfectly safe for autonomous trading. Downgrade driven by 4 hard crash bugs + evolution sandbox broken)

---

## CRITICAL Findings (Fix Before Any Live Trading)

### C1. `backtest.py` — Broken Import Path (Will Crash at Runtime)
**File:** `src/dashboard/api/backtest.py:182`  
**Severity:** CRITICAL — Crash on use  

```python
# BROKEN — no exchange/binance directory exists
from src.infrastructure.exchange.binance.client import BinanceFuturesClient
```

The correct path (used everywhere else) is `src.infrastructure.binance.client`. This crashes `GET /api/backtest/preview` immediately when `mode="range"`.

---

### C2. `fetch_liquidations()` Always Returns `None` — Silent Data Gap
**File:** `src/infrastructure/binance/client.py:223`  
**Severity:** CRITICAL — Strategy blind spot  

```python
def fetch_liquidations(self, ...) -> Optional[List[LiquidationData]]:
    return None  # Hardcoded — never implemented
```

The entire liquidation radar (STRUCTURAL_APPROACH magnet detection, liquidation heatmap on charts, LLM situational awareness of leverage clusters) receives **zero data**. This is one of the most important market structure signals completely absent. The Sniper's liquidation cluster proximity triggers never fire.

---

### C3. CVD Calculation Uses Incorrect Formula
**File:** `src/analyzer/market_observer.py:516-525`  
**Severity:** CRITICAL — All flow signals distorted  

```python
cvd_vol_delta += (tb - (v - tb))  # = 2*tb - v — WRONG
```

`v - tb` (volume minus taker buy base) is NOT taker sell volume — it includes maker volume. Standard CVD = taker_buy - taker_sell. This systematically distorts `cvd_intensity_ratio`, which feeds the Sniper's FLOW_ASYMMETRY triggers, the Critic's `HAS_FLOW_DOMINANCE` / `HAS_FLOW_OPPOSITION` checks, and the Session Agent's directional bias.

**Fix:** Use `taker_buy_quote` / `quote_volume` ratio or: `cvd_delta = (taker_buy_quote / quote_volume) - ((quote_volume - taker_buy_quote) / quote_volume)` simplified to `(2 * taker_buy_quote - quote_volume) / quote_volume`.

---

### C4. Order Entry — Pivot Creates Naked Position Window
**File:** `src/agent/order_executor.py:140-176`  
**Severity:** CRITICAL — Financial loss risk  

The "Pivot-Preserve" case cancels ALL orders (including the existing position's OCO stop-loss protection), then places a new OCO. Between cancel and re-place, the position is **completely naked** — no stop loss. A violent market move in that sub-second window causes unlimited loss.

**Fix:** Place the new OCO FIRST, then cancel the old one. Or use OCO modification if the API supports it.

---

### C5. `MarginClient` — Wrong `sideEffectType` on SELL Orders
**File:** `src/infrastructure/binance/margin_client.py:195, 214`  
**Severity:** CRITICAL — Orders may be rejected  

```python
sideEffectType="MARGIN_BUY"  # Hardcoded regardless of side
```

For SELL orders (closing longs or opening shorts), `MARGIN_BUY` is semantically wrong. The Binance API expects `AUTO_REPAY` for sells (repaying borrowed assets). This could cause order rejections in production.

---

### C6. `GeminiAdapter` — `elif` Blocks `response_mime_type` When Tools Present
**File:** `src/infrastructure/ai/gemini_adapter.py:49-53`  
**Severity:** CRITICAL — JSON parsing failures  

```python
if tools:
    gen_config["tools"] = ...
elif response_json:   # ← Should be `if`, not `elif`
    gen_config["response_mime_type"] = "application/json"
```

When both `tools` AND `response_json=True` are passed (normal mode: function calling + JSON output), `response_mime_type` is never set. The model may not output parseable JSON. This affects every Session/Critic inference cycle.

---

### C7. `session_run.py` / `backtest.py` — PID Is Dashboard Server, Not Task
**File:** `src/dashboard/api/session_run.py:166`, `backtest.py:432`  
**Severity:** CRITICAL — Stale-lock detection broken  

```python
"pid": os.getpid()  # Returns FastAPI server PID, not background thread
```

The `_is_pid_alive()` check always returns `True` (server is alive). A crashed background task leaves a permanent `"running": True` status. No new session/backtest can start until the server restarts or the status file is manually deleted.

---

### C8. LiquidationEstimator — Broken SMA Produces Garbage First N Elements
**File:** `src/analyzer/liquidation_estimator.py:139-144`  
**Severity:** CRITICAL — False-positive accumulation signals  

```python
ret = np.cumsum(data, dtype=float)
ret[period:] = ret[period:] - ret[:-period]
return ret / period  # First period-1 elements are partial cumsum/period, NOT valid SMA
```

The first `period-1` elements are garbage (partial cumulative sums divided by period). These feed into the accumulation mask (`volumes > vol_ma * ratio`), causing **false-positive liquidation cluster detection** on every observation. The LLM sees phantom liquidation zones.

---

### C9. `pipeline_utils.py:79` — Undefined `logger` Variable (NameError Crash)
**File:** `src/utils/pipeline_utils.py:79`  
**Severity:** CRITICAL — Crashes daemon startup on missing config  

```python
logger.warning("Failed to load %s: %s — falling back to empty config", filepath, e)
```

`logger` is **never imported or defined** in `pipeline_utils.py`. Any call to `load_global_config()` that encounters a missing or malformed `global_config.yaml` will crash with `NameError: name 'logger' is not defined`. This is called on every session, backtest, audit, and sniper startup.

---

### C10. `evolver_sandbox.py:124-129` — Missing Required `symbol` Argument (Evolution Broken)
**File:** `src/agent/evolver_sandbox.py:124-129`  
**Severity:** CRITICAL — Evolution sandbox validation entirely broken  

```python
orchestrator = BinaryStarOrchestrator(
    config_dict=proposed_config,
    api_key=self.api_key,
    data_root=self.data_root,
    instruction_overrides=instruction_overrides
    # ← Missing: symbol=symbol
)
```

`BinaryStarOrchestrator.__init__()` requires `symbol` as a **positional parameter with no default**. The sandbox constructs it without `symbol`, causing `TypeError` on every call to `replay_audit_with_patch()`. The entire evolution sandbox batch-validation path is broken — evolution proposals are never properly validated.

**Fix:** Add `symbol=symbol` (the variable is already in scope from line 49).

---

### C11. `run_session.py:278` — AttributeError on `args.sampling_mode` (Backtest Crash)
**File:** `run_session.py:278` (or equivalent standalone)  
**Severity:** CRITICAL — Crashes backtest on startup  

```python
logger.info(f" => ADOPTED: --start '{args.start}', ..., --sampling-mode {args.sampling_mode}")
```

`sampling_mode` is **never defined** as a CLI argument. When `--start` is set (backtest mode), this log line raises `AttributeError` and crashes before any analysis occurs.

---

### C12. `scripts/calculate_qty.py` — Missing `sys.path` Injection (Standalone Crash)
**File:** `scripts/calculate_qty.py:70`  
**Severity:** CRITICAL — Script crashes when run standalone  

The script imports `from src.config.symbol_resolver import ...` but never injects the project root into `sys.path`. Every other standalone entry point (`run.py`, `clean_neutral_sessions.py`) does this. The script crashes with `ModuleNotFoundError` unless the package is pip-installed or `PYTHONPATH` is manually set.

---

## HIGH Severity Findings

### H1. VolumeProfile — `df.iterrows()` on Every Hot-Path Observation
**File:** `src/analyzer/volume_profile.py:84-96`  
**Severity:** HIGH — CPU waste  

Python `iterrows()` loop over 500+ candles × 300 bins on every observation. Across backtests with thousands of sessions, this dominates CPU. Vectorizable with `np.digitize` + `np.add.at`.

---

### H2. Same Kline Processing Executed Twice Per Observation
**File:** `src/analyzer/market_observer.py:411-412` and `697-698`  
**Severity:** HIGH — 2× CPU waste  

`process_klines()` is called in `refine()` AND again in `_generate_snapshots()` for the same raw data. This doubles ATR recalculation cost and exacerbates the `iterrows()` problem (H1).

---

### H3. Volatility Normalization Uses Linear Instead of √Time Scaling
**File:** `src/analyzer/market_observer.py:441`  
**Severity:** HIGH — Chaos detection impaired  

```python
ratio = interval_seconds(macro) / interval_seconds(micro)  # e.g., 3600/900 = 4
volatility_expansion_index = atr_n / (atr_m / ratio)  # Divides by 4
```

Volatility scales with **√time**, not linearly. With 1h vs 15m, ATR should be normalized by √4 = 2, not 4. The code **understates** volatility expansion by ~2× in this case, meaning the system fails to detect chaos regimes when it should.

---

### H4. `project_holding_time` — Division-by-Zero Risk
**File:** `src/utils/math_utils.py:246-252`  
**Severity:** HIGH — Crash in edge case  

`effective_velocity = effective_velocity_per_atr * atr`. If both `min_velocity_floor=0` and `normalized_velocity=0`, `effective_velocity_per_atr = max(0, 0, 1e-9) = 1e-9`. With tiny ATR (stablecoin), `effective_velocity` could be 0 in float arithmetic. Add explicit guard.

---

### H5. OpenAI Client — No Default Timeout (Requests Can Hang Forever)
**File:** `src/infrastructure/ai/_openai_helpers.py:178`  
**Severity:** HIGH — Session hang risk  

```python
self._client = OpenAI(api_key=..., base_url=...)  # No timeout specified
```

The OpenAI SDK defaults to `timeout=None` (indefinite). If `http_timeout` is not passed at call time, the request hangs forever. This can block the entire session pipeline.

---

### H6. Config Re-read from Disk on Every Order Operation
**File:** `src/infrastructure/binance/margin_client.py:156-176`  
**Severity:** HIGH — Performance + I/O storm  

`_get_precisions()` opens + parses `global_config.yaml` + `symbol_config.yaml` on every call. Called from `execute_market_close`, `place_limit_order`, `place_oco_order`, `place_otoco_order`. In a 2-minute sniper pulse loop, that's ~720 redundant YAML parses per day per symbol.

---

### H7. `CongestionController` — Not Thread-Safe
**File:** `src/utils/rate_limiter.py:31-47`  
**Severity:** HIGH — Rate limit bypass under concurrency  

Read-check-then-write on `last_call_time` with no locking. Under concurrent access (dashboard + sniper), rate limiting is bypassed.

---

### H8. Trigger — State Locks Consumed for Non-Firing Signal Types
**File:** `src/sniper/trigger.py:96-101` and `evaluate()`  
**Severity:** HIGH — Missed trading signals  

`_check_state_lock()` is called during evaluation of ALL signal types, but only the STRONGEST type fires. Other types had their locks "consumed" without triggering. On subsequent pulses, those conditions are locked out even though they never fired.

---

### H9. `RegimePhysicsConfig` — Constructed in Two Different Places
**Files:** `binary_star_orchestrator.py:395-433` and `math_fact_checker.py:66-90`  
**Severity:** HIGH — Potential divergence  

Both the orchestrator (`_inject_regime_benchmarks`) and the `MathFactChecker.verify()` independently construct `RegimePhysicsConfig` from the same config values. If these ever diverge (config change, refactor), the unit-ATR benchmarks injected into the observation will differ from the math verification calculations.

---

### H10. Time-Stop Uses Stale Holding-Time Projection
**File:** `src/agent/order_executor.py:375-387`  
**Severity:** HIGH — Premature/overdue position close  

`projected_holding_hours` is computed ONCE when the trade opens. If volatility changes, the physical time to TP changes but the time-stop deadline doesn't. A volatility spike means the trade should close faster; a volatility collapse means it should run longer. Neither adjustment happens.

---

### H11. `BinanceMarginClient.get_symbol_position` — Brittle String Manipulation
**File:** `src/infrastructure/binance/margin_client.py:124`  
**Severity:** HIGH — Wrong base asset extraction  

```python
base_asset = symbol.replace("USDT", "")  # Fails for BTCUSDC, BTCTUSD, etc.
```

Also hardcodes `quote_asset="USDT"` and fetches the entire margin account on every call.

---

### H12. `backtest.py:270-272` — Session Filename Never Captured
**File:** `src/dashboard/api/backtest.py:270-272`  
**Severity:** HIGH — Backtest UI broken  

```python
if result and not isinstance(result, dict):
    pass  # Empty block — filename never stored
```

The backtest UI cannot link individual samples to generated session reports. Users cannot click through to see individual session details.

---

### H13. Fast-Pass Bypass Can Skip Critical Audits After Market Regime Change
**File:** `src/agent/debate_loop.py:165-226`  
**Severity:** HIGH — Missed trade validation  

The `_evaluate_critic_fast_pass` amnesty clause returns PASS when a prior TERMINAL veto exists, skipping the critic entirely. If the market has materially changed since the TERMINAL veto round, the NEUTRAL decision gets no adversarial review.

---

### H14. Binance Pagination Returns Partial Data Silently
**File:** `src/infrastructure/binance/client.py:110-145`  
**Severity:** HIGH — Data integrity  

When pagination fails mid-stream, partial klines are returned with only a log warning. All downstream analysis (regime, volume profile, agent decisions) operates on incomplete data with no error signal.

---

### H15. `_calculate_target_qty` Applies BTC Equity Conversion to Non-BTC Pairs
**File:** `src/agent/order_executor.py:601-640`  
**Severity:** HIGH — Documentation gap (risk of over-leverage)  

Uses `total_net_asset_of_btc * BTC_price` for ALL symbols. If BTC pumps, non-BTC position sizes double. While technically correct for cross-margin accounts, the behavior is not documented and could surprise operators.

---

### H16. `order_executor.py:113-114` — `current_price` May Be `None`, Causing TypeError
**File:** `src/agent/order_executor.py:113-114`  
**Severity:** HIGH — Crash in pivot execution path  

```python
current_price = self.client.get_ticker_price(symbol)
is_overshot = (current_direction == "SHORT" and current_price <= entry_price) or ...
```

`get_ticker_price` can return `None` or `0.0` on API error. Comparing `None <= entry_price` raises `TypeError`. `guardian_check` correctly guards against this (line 391), but `sync_with_opinion` does not.

---

### H17. `order_executor.py:456` — Unsafe `get_symbol_position()` Access in Trailing Stop
**File:** `src/agent/order_executor.py:456`  
**Severity:** HIGH — AttributeError in OCO migration path  

```python
qty=abs(self.client.get_symbol_position(symbol).net_qty)
```

If `get_symbol_position` returns `None`, accessing `.net_qty` raises `AttributeError`. This is in the cancel-all/re-place-OCO critical path — failure leaves the position completely naked (no stop-loss).

---

### H18. `order_executor.py` — Zero `projected_holding_hours` Triggers Immediate Time-Stop
**File:** `src/agent/order_executor.py:375-380`  
**Severity:** HIGH — Positions closed immediately after fill  

The orchestrator sanitizes `tactical_parameters` from math checker output. If `holding_time_verification` is empty, `projected_holding_hours` defaults to 0. In `_migrate_trailing_stop`, `time_limit = 0 * 1.5 = 0`. Any filled position is immediately time-stopped on the next guardian pulse — closed before it can reach target.

---

### H19. `order_executor.py` — No NEUTRAL Direction Guard in `sync_with_opinion`
**File:** `src/agent/order_executor.py:45`  
**Severity:** HIGH — Undefined behavior on NEUTRAL opinion  

The function accepts `opinion_direction: str` but only handles LONG/SHORT. If called with "NEUTRAL" (a valid AI output), the code enters undefined behavior — attempting to place orders with an invalid side.

---

### H20. `base_agent.py:254` — Simulated Tool Calls Silently Dropped When "opinion" Present
**File:** `src/agent/base_agent.py:254`  
**Severity:** HIGH — Tool results silently lost  

```python
if simulated and "opinion" not in parsed:
    # dispatch simulated tools
    continue
return parsed
```

If the LLM response contains BOTH simulated function calls AND an "opinion" field, the tool calls are silently discarded. The model's expected tool results are never generated, potentially causing stale/incomplete decisions.

---

### H21. `debate_loop.py:61-73` — Math Fact Check Executed BEFORE Type Validation
**File:** `src/agent/debate_loop.py:61-73`  
**Severity:** HIGH — Wasted computation, potential AttributeError  

`math_checker.verify(last_plan, observation)` is called on line 61, but `last_plan` isn't validated as a dict until line 65. If the LLM returned a string (malformed response), `verify()` accesses `.get()` on it — `AttributeError`, caught by the broad except and returned as `VERIFICATION_FAILURE`. The math check is wasted effort.

---

### H22. `debate_loop.py:168-180` — Amnesty Clause Too Broad
**File:** `src/agent/debate_loop.py:168-180`  
**Severity:** HIGH — Bypasses adversarial review for all subsequent rounds  

The amnesty fast-pass triggers on ANY prior TERMINAL veto in history. Once one occurs, EVERY subsequent NEUTRAL plan gets fast-passed for the rest of the debate — even if the market has materially changed. Combined with `defer_to_synthesis: True`, it forces the debate through all `max_rounds` with no real adversarial review.

---

### H23. `fitness_evaluator.py:27-28` — Config Defaults Out of Sync With YAML
**File:** `src/utils/fitness_evaluator.py:27-28`  
**Severity:** HIGH — Silent fitness score corruption  

```python
neutral: float = 60.0   # code default
sl_hit: float = 15.0    # code default
```

But `global_config.yaml` has `neutral: 70.0` and `sl_hit: 5.0`. If the YAML is ever missing or the sandbox block absent, the class defaults silently apply — producing completely different fitness scores (NEUTRAL goes from 70→60, SL_HIT goes from 5→15). Every other default matches between code and YAML.

---

### H24. `run_sniper.py` / `run_session.py` — Redundant Config Disk Reads
**Files:** Multiple (`run_sniper.py:96`, `run_evolution.py:181-182`)  
**Severity:** HIGH — I/O storm  

`load_global_config()` is called redundantly in `run_forever()` when `self.global_cfg` was already loaded in `__init__`. Similar patterns in `run_evolution.py` (loaded but never used). Combined with per-order config re-reads (H6), the system does 1,000+ redundant YAML parses per day.

---

## MEDIUM Severity Findings

### M1. `MarketRegime.analyze` Returns Indistinguishable Defaults on Data Failure
**File:** `src/analyzer/market_regime.py:142-144`  

Returns `squeeze_factor=1.0, trend_intensity=0.0, volume_participation_ratio=1.0` — all "normal" values — when data is insufficient. The LLM cannot distinguish "sensor failure" from "perfectly balanced market."

---

### M2. `AuditAssembler` — SL Hit Takes Priority Over TP Hit
**File:** `src/analyzer/audit_assembler.py:191-209`  

If a single candle touches both SL and TP, SL always wins (due to `elif` ordering). This biases audit metrics pessimistically. Without tick data, the correct approach is to use open-close ordering relative to SL/TP.

---

### M3. `ChartGenerator` — Floating-Point Equality in Peak Detection
**File:** `src/analyzer/chart_generator.py:61-62`  

```python
highs = df['high'].rolling(...).max() == df['high']  # FP equality misses valid peaks
```

Use `np.isclose()` with tolerance.

---

### M4. `SniperSampler` — O(N) Exchange API Calls in Sampling Loop
**File:** `src/analyzer/simulation_sampler.py:57-79`  

Each backtest sample makes a separate `scout()` call hitting the exchange API. For 336 samples, that's 336 serial API calls. Should batch-fetch.

---

### M5. `AuditController` — First `calculate_outcome` Call Is Discarded
**File:** `src/analyzer/audit_controller.py:189-199` and `231-241`  

Full outcome calculation is run once for maturity check, result discarded, then run again for actual audit. Wasted computation.

---

### M6. `TopographyEngine` Closes Exchange Client It May Not Own
**File:** `src/analyzer/topography_engine.py:80`  

If the client was injected (shared), calling `.close()` destroys it for all other consumers.

---

### M7. `funding_rate_lookback_hours` — Float Truncated to Int, Losing Data
**File:** `src/analyzer/market_observer.py:379`  

```python
int(cfg.funding_rate_lookback_hours)  # 1.5h → 1h, losing 33% of lookback
```

Use `round()` or compute in float before converting.

---

### M8. Tool Parameter Conversion Drops `enum`/`default`/Constraints
**File:** `src/infrastructure/ai/_openai_helpers.py:84-91, 111-116`  

Only `type` and `description` are extracted from parameter schemas. `enum`, `minimum`, `maximum`, `items`, and nested `properties` are silently dropped. LLMs won't see parameter constraints.

---

### M9. `build_messages` Silently Skips Unrecognized Content
**File:** `src/infrastructure/ai/_openai_helpers.py:73`  

Unrecognized message types are silently dropped with only a debug log. New content formats would be lost without detection.

---

### M10. Dashboard — No Authentication Enforcement on API Routes
**File:** `src/dashboard/server.py:75-85` and API routers  

`_load_users()` and `_get_user_permissions()` exist but are **never called** by any route. All write endpoints (`/api/sniper/start`, `/api/sniper/stop`, `/api/session/run`) are publicly accessible.

---

### M11. `live.html` — `innerHTML` From API Data Without Sanitization
**File:** `src/dashboard/templates/live.html:363-392`  

Guardian status data injected via `innerHTML` without escaping. While the data source is local, corrupted status files could execute scripts.

---

### M12. `live.html` — Paste Events Bypass Client-Side Input Validation
**File:** `src/dashboard/templates/live.html:55-56`  

Balance input `onkeydown` filter is bypassed by paste. Server-side `NaN` check (`NaN <= 0` is `False`) lets non-numeric values through.

---

### M13. `audits.py` — ThreadPoolExecutor Blocks Sync Endpoint
**File:** `src/dashboard/api/audits.py:399-401`  

With many session files and only 4 workers, audit batch processing blocks the request thread for minutes. Starves FastAPI's default thread pool under concurrent usage.

---

### M14. `audits.py` — Calmar Ratio Uses Non-Annualized Total Return
**File:** `src/dashboard/api/audits.py:157`  

Calmar = total_return / max_drawdown instead of annualized_return / max_drawdown. Inflates ratio for long backtests, understates for short ones.

---

### M15. `sessions.py` — Fragile Timestamp Format Detection
**File:** `src/dashboard/api/sessions.py:63-66`  

String check `"_" in t0_str and "-" not in t0_str` matches unintended formats. Should use regex.

---

### M16. `session_html_renderer.py` — Raw LLM Output in HTML Without Escaping
**File:** `src/dashboard/session_html_renderer.py:144-148`  

LLM-generated reasoning text placed directly into `<pre>` tags. HTML entities in LLM output break email layout.

---

### M17. `run.py` Audit Mode Uses Unbounded `cpu_count()` Workers
**File:** `run.py:188-189`  

64-core machine → 64 processes, each making independent API calls. Triggers Binance rate limits. Cap at 4-8.

---

### M18. `BaseAgent._prepare_prompt` — Unresolved Template Variables Sent to LLM
**File:** `src/agent/base_agent.py:102-111`  

Detects unresolved `{placeholders}` but **proceeds to send them to the LLM anyway**. The LLM sees literal `{max_rounds}` text.

---

### M19. `_get_retryer` — 30-Day Boundary Uses `<=` Instead of `<`
**File:** `src/infrastructure/binance/client.py:342`  

Exactly-30-day-old data passes client check but may be rejected by Binance's strict `<` API window. Intermittent failures at boundaries.

---

### M20. `fetch_order_book` Uses Local Clock Instead of Exchange Server Time
**File:** `src/infrastructure/binance/client.py:209`  

`datetime.now()` for order book timestamp. System clock drift causes inaccurate timestamps.

---

## LOW Severity / Quality Findings

### L1. `wick_skew_instant` Uses Single-Candle Data (Noisy)
**File:** `src/analyzer/market_observer.py:435`  

### L2. `wick_skew` Fallback Duplicated Between MarketObserver and MarketRegime
Two slightly different implementations of the same wick skew metric.

### L3. VolumeProfile — VAH/VAL Reported as Bin Edges, Not Midpoints
**File:** `src/analyzer/volume_profile.py:135-136` — Systematic ~0.05% downward bias.

### L4. `bin_distance` Calculation Inaccurate With Sparse Profiles
**File:** `src/analyzer/volume_profile.py:182-187`  

### L5. ChartGenerator — Hardcoded 20% Distance Filter for Liquidation Targets
**File:** `src/analyzer/chart_generator.py:218` — Magic number not configurable.

### L6. `math_fact_checker.py` — No Validation of `tactical_parameters` Structure
**File:** `src/analyzer/math_fact_checker.py:44-46` — Missing keys silently become 0.

### L7. `EmailDispatcher` — No SMTP Connectivity Check at Init
**File:** `src/infrastructure/notifications/base_notifier.py:126-128`  

### L8. `MarginOrder.order_id` Is `int` — JS Precision Loss Above 2^53
**File:** `src/infrastructure/exchange/models.py:86`  

### L9. `GeminiCacheManager` and `GeminiAdapter` Have Competing `delete_cache`
Architectural confusion — two delete paths.

### L10. `BinanceFuturesClient.close()` Is Misleading No-Op
**File:** `src/infrastructure/binance/client.py:344-346`  

### L11. `clean_json_text` — Single-Block Extraction Only
**File:** `src/infrastructure/ai/_openai_helpers.py:144-150`  

### L12. No Startup Import Validation for Active AI Provider
**File:** `src/infrastructure/ai_factory.py:23, 35, 47` — Lazy imports fail at runtime.

### L13. `server.py` — `data_root` Query Param on `/performance` Is Dead Code
**File:** `src/dashboard/server.py:99`  

### L14. `sniper_run.py` — SIGTERM Doesn't Wait for Process Termination
**File:** `src/dashboard/api/sniper_run.py:138`  

### L15. No Print Stylesheet in Dashboard CSS

### L16. Test Coverage Gaps: No tests for SniperScout, SniperTrigger, MarginOrderExecutor.guardian_check, DebateLoop fast-pass, MarketObserver end-to-end

### L17. Hardcoded `"USDT"` Suffix Assumption in Multiple Places

### L18. `funding_rate_limit` Calculation Uses Unexplained Magic Number 8
**File:** `src/analyzer/market_observer.py:363`  

---

## Prompt & Strategy Logic Review

### Prompt Analysis — `binary_star.md` (Shared System Preamble)

**Findings:**
- **P1.** `IS_SQUEEZING` uses `squeeze_threshold` (1.0 from config) but `IS_CHAOS` uses `volatility_extreme_ratio` (2.2). These are correctly templated — no bug here, but the thresholds should be reviewed for consistency (squeeze < 1.0 triggers more easily than chaos > 2.2).
- **P2.** The LOGIC_MACROS section defines `HAS_ABSORPTION_RISK` without templating variables — it's hardcoded as `oi_delta_micro < 0 AND abs(cvd_intensity_ratio) > {cvd_intensity_extreme}`. The `cvd_intensity_extreme` variable IS templated correctly.
- **P3.** The visual chart description (section 5) is extremely detailed. With ~500 words describing chart layout, this consumes significant context tokens. Consider moving to a concise reference.

### Prompt Analysis — `session.md` (Session Analyst)

**Findings:**
- **P4.** **CRITICAL LOGIC DIVERGENCE:** Line 55 instructs the agent to manually calculate `projected_holding_hours = abs(take_profit - entry) / atr_macro * unit_atr_holding_hours`. But `unit_atr_holding_hours` was injected by `_inject_regime_benchmarks()` with dilation already applied. Then `MathFactChecker.verify()` ALSO calculates `project_holding_time()` using the Python `project_holding_time()` function. In `_finalize_and_sanitize()`, the Python-verified value OVERWRITES the agent's manual calculation. So the agent's calculation is **always discarded** — but the agent spends reasoning tokens on it. The prompt should explicitly say "calculate a rough estimate; the system will replace it with the Python-verified value."
- **P5.** The CONFIDENCE CALCULUS (lines 100-112) is a 4-dimension penalty system with max 30+30+20+20=100. The Critic's Crucible dimension awards 0 points during planning or if any CONSTRUCTIVE/TERMINAL challenge remains. This means **every first-round plan has max possible score of 80** (since Critic's Crucible = 0). The prompt doesn't explicitly state this, which may confuse the LLM.
- **P6.** "TOOL ERROR FALLBACK: If MathTools returns an error, immediately abort to NEUTRAL" (line 25) — this is aggressive. A transient API error in the math tool causes trade abortion. Should allow 1 retry.
- **P7.** The SHIELD LAW (line 48) says "anchor MUST sit strictly BETWEEN entry and stop_loss." But the MOMENTUM EXEMPTION (line 50) relaxes this when `IS_TREND_STRONG` and POC > `poc_gravity_atr_distance` (3.5 ATR). This exemption is reasonable but creates an asymmetry: in strong trends with distant POC, the anchor requirement is waived, potentially allowing stops in liquidity vacuums.

### Prompt Analysis — `critic.md` (Critic Agent)

**Findings:**
- **P8.** The CRITIC_CODES table has an **implicit priority inversion**: `[FLOW_VIOLATION]` (fighting CVD flow direction) is CONSTRUCTIVE, but `[RETAIL_LONG_SQUEEZE]` / `[RETAIL_SHORT_SQUEEZE]` (fighting retail positioning) is TERMINAL. This means the system treats "retail is crowded" as a harder veto than "CVD flow is opposite." This is a deliberate design choice (retail positioning is a contrarian indicator) but the asymmetry should be explicitly documented.
- **P9.** `IS_OVEREXTENDING` definition (line 38) correctly uses: `NOT (IS_TREND_STRONG AND HAS_FLOW_DOMINANCE)` as the exemption. This allows aggressive trend-following entries far from POC when both trend AND flow confirm.
- **P10.** `IS_HOLDING_TOO_LONG` (line 39) uses `max_holding_hours * temporal_weight_factor` from the math verification. But `temporal_weight_factor` comes from the regime scalars which are computed from the **current** regime. If the regime changes mid-trade, the threshold changes but the elapsed time doesn't reset. This is actually reasonable behavior.
- **P11.** THE NEUTRALITY PARADOX protocol is well-designed — it prevents the Critic from endlessly challenging NEUTRAL decisions when structural reasons justify inaction.

### Prompt Analysis — `evolver.md` (Meta-Evolver)

**Findings:**
- **P12.** `IS_OVERFIT_RISK`: "Historical fix would invalidate > 5% of Pristine success records." The LLM has **no way to actually validate this** — it can't run backtests. This is a heuristic guideline that the LLM must approximate from qualitative analysis of the audit reports. The Evolver may overfit or underfit based on LLM hallucination of the "5% threshold."
- **P13.** The `SEMANTIC_REFINEMENT` anchor rule (line 102) requires "character-for-character, byte-perfect copy" of prompt text. If the prompts have been modified since the audit was generated (e.g., by a previous evolution run), the EVOLVER's anchor won't match and the patch will silently fail with "NO MATCH."
- **P14.** The `DE_SENSITIZATION` deadlock-breaking action is triggered when `max_rounds` was exceeded. Current `max_rounds=2`, which means 2 rounds with TERMINAL/CONSTRUCTIVE veto = deadlock. With only 2 rounds configured, deadlocks reach cold synthesis quickly. The evolvers' DE_SENSITIZATION path is unlikely to ever trigger.

### Prompt→Code Integration Issues

- **P15.** Session agent is told to use `unit_atr_holding_hours` for time projection (session.md line 55), which is injected by `_inject_regime_benchmarks()`. But this function only runs ONCE at the start of `execute_flow()`. If the debate goes multiple rounds and the market regime changes between rounds, the injected `temporal_physics` in the observation is stale. The agent would use stale velocity estimates.
- **P16.** The critic prompt's `HAS_ANCHOR_VIOLATION` check references `near liquidation_cluster AND (stop_loss >= cluster OR stop_loss <= cluster)` but the observation JSON's `liquidation_clusters` structure is always empty (since `fetch_liquidations()` returns None — see C2). This entire check path is a no-op.

---

## Configuration & YAML Review

### `strategy_config.yaml`

- **CFG1.** `volume_profile_value_area_width: 0.86` — 86% Value Area is unusually wide (standard is 68-70%). This captures more volume but makes VAH/VAL boundaries less significant as support/resistance.
- **CFG2.** `squeeze_threshold: 1.0` — BB_width / KC_width < 1.0 means Bollinger Bands are inside Keltner Channels. This is the standard definition. The `squeeze_audit_threshold: 0.85` is a separate, tighter threshold used only by the Critic's fast-pass check.
- **CFG3.** `min_rr_ranging: 1` and `min_rr_trending: 1.2` — Ranging allows 1:1 RR (50% win rate = breakeven). Trending requires 1.2:1. With `chaos_rr_discount: 0.35`, chaos minimum becomes 0.65 for ranging and 0.78 for trending — appropriately aggressive for survival mode.
- **CFG4.** `max_holding_hours: 72.0` with `temporal_weight_climax: 0.3` → effective max hold in climax = 21.6 hours. With `time_stop_multiplier: 1.5` → time-stop at 32.4 hours. Reasonable.
- **CFG5.** `wick_skew_fallback: 0.5` — When a candle has zero range (doji), the fallback assumes neutral wick skew. A doji at a boundary is actually a significant signal — a more informative fallback would look at the prior candle or use `None`.

### `global_config.yaml`

- **CFG6.** `active_provider: "deepseek"` — DeepSeek is used as the primary LLM. DeepSeek does NOT support vision (confirmed by `supports_vision: false` in the config). This means chart images (VisualPart) are **never sent to the LLM**. The prompt instructions reference `[VISUAL_CONTEXT]` extensively, but the agent never actually sees the charts. **This is a significant strategy gap** — the Session and Critic agents are making decisions without the visual ground truth they're instructed to use.
- **CFG7.** `max_rounds: 2` — Only 2 debate rounds. This means: Round 1 (planning + audit), Round 2 (revision + audit). If Round 2 fails, cold synthesis produces the final decision. This is tight but cost-efficient. The risk is that complex market setups need 3+ rounds to converge.
- **CFG8.** `risk_per_trade: 0.004` (0.4% of equity). Conservative. With 0.4% risk per trade and a 1.2 RR minimum, the breakeven win rate is ~45%.
- **CFG9.** `net_qty_tolerance: 0.00001` — This is the minimum BTC quantity to consider a position "FLAT." At $100k/BTC, this is $1. Reasonable, but if BTC price falls to $10k, this becomes $0.10 which might be too tight (rounding errors could show phantom positions).
- **CFG10.** Sniper `pulse_interval_minutes: 2.0` with `pulse_cooldown_multiplier: 2.5` → cooldown = 5 minutes. With `chaos_cooldown_multiplier: 2.0` → chaos cooldown = 10 minutes. The `state_lockout_hours: 8.0` blocks retriggers for a full trading session.
- **CFG11.** Guardian trailing levels: Level 1 at 1.5 ATR profit (SL→breakeven), Level 2 at 2.5 ATR (SL→entry+0.5 ATR), Level 3 at 4.0 ATR (SL→entry+1.5 ATR). These are aggressive — a position needs to move 1.5× ATR before even reaching breakeven. Many trades will be stopped out before trailing activates.

### `symbol_config.yaml`

- **CFG12.** BTCUSDT `sl_slippage_buffer: 10.0` — A $10 buffer on BTC stop-loss. At $100k BTC, this is 0.01% — very tight. During volatility, this could cause the stop limit to not fill.
- **CFG13.** XAUTUSDT `sl_slippage_buffer: 0.5` — $0.50 buffer on gold. At ~$3000 XAUT, this is 0.017% — similarly tight.
- **CFG14.** XAUTUSDT overrides: `cvd_divergence_tick_delta: 0.18` and `cvd_impulse_tick_delta: 0.25` (lower than BTC defaults of 0.22/0.35). This makes XAUT more sensitive to flow signals — appropriate for a thinner market.

---

## Architecture & Strategy Observations

### Strengths

1. **Dependency Injection Everywhere** — `AbstractAIClient`, `AbstractExchangeClient`, config dataclasses all injected. Clean testing seams.
2. **Frozen Dataclasses for Config** — Immutable configuration prevents accidental mutation.
3. **Domain Exception Hierarchy** — `SingularityError → AgentInferenceError → ...` enables precise error handling.
4. **Adversarial Debate Architecture** — Session→Critic with MathFactChecker verification. Genuinely innovative.
5. **Guardian System** — Progressive trailing stop + time-based stop + emergency fallback. Professionally designed.
6. **Provider-Agnostic AI Layer** — `VisualPart`, `AIResponse`, `ToolCall` abstractions decouple agents from SDKs.
7. **Config Resolution Stack** — `base + symbol.overrides → resolved` is clean and testable.

### Architecture Risks

1. **Visual Context Disabled:** DeepSeek (`supports_vision: false`) means chart images are NEVER sent to the LLM. The Session and Critic prompts extensively reference `[VISUAL_CONTEXT]` but the agents operate text-only. The system is effectively running blind to the charts it generates.
2. **Single Point of Failure: AI Inference.** No fallback to deterministic strategy on provider outage.
3. **No Paper Trading / Fast Backtest Mode.** All backtesting goes through the full LLM pipeline, incurring API costs.
4. **Configuration Complexity:** ~150+ parameters across 4 YAML files. A single misconfigured value could cause financial loss.
5. **Binary Star Protocol Assumes Rational Debate.** If both agents hallucinate in the same direction, the adversarial safeguard fails. MathFactChecker only verifies arithmetic, not directional correctness.
6. **No Account-Level Drawdown Circuit Breaker.** The strategy intent says "LARGE DRAWDOWNS ARE ABSOLUTELY PROHIBITED" but the system has no mechanism to detect or enforce this.

---

## Data Flow — End-to-End Audit

```
Binance API
  → BinanceFuturesClient (kline/OI/LS/funding/liquidation fetches)
    → MarketDataLoader.collect() [BUG: fetch_liquidations → None]
      → RawMarketData
        → MarketMetricsRefiner.refine()
          → VolumeProfileAnalyzer.process_klines [PERF: iterrows]
          → MarketRegimeAnalyzer.analyze
          → LiquidationEstimator.synthesize_clusters [BUG: broken SMA]
          → _derive_price_dynamics [BUG: linear ATR scaling]
          → _derive_sentiment [BUG: CVD formula wrong]
            → ProcessedMarketMetrics
              → MarketObserver._package_observation()
                → observation dict (JSON) [NOTE: visual_context stripped]
                  → BinaryStarOrchestrator.execute_flow()
                    → _inject_regime_benchmarks [DUPLICATE: RegimePhysicsConfig]
                    → DebateLoop.run()
                      → SessionAgent ↔ CriticAgent (LLM debate)
                        → MathFactChecker.verify() [DUPLICATE: RegimePhysicsConfig]
                    → _finalize_and_sanitize [OVERWRITES agent time projection]
                    → final_decision
                      → MarginOrderExecutor.sync_with_opinion()
                        → BinanceMarginClient [BUG: wrong sideEffectType]
                          → Guardian [ISSUE: stale holding projection]
                            [RACE: naked window on pivot OCO]
```

---

## Summary Table — All Findings

| # | Severity | Area | Issue |
|---|----------|------|-------|
| C1 | **CRITICAL** | Dashboard | `backtest.py` broken import path — crashes at runtime |
| C2 | **CRITICAL** | Exchange | `fetch_liquidations()` always returns None — blind spot |
| C3 | **CRITICAL** | Analysis | CVD calculation uses wrong formula — all flow signals distorted |
| C4 | **CRITICAL** | Order Exec | Pivot cancel-before-place creates naked position window |
| C5 | **CRITICAL** | Margin | Wrong `sideEffectType` on SELL orders — may reject |
| C6 | **CRITICAL** | AI | `elif` blocks `response_mime_type` when tools present |
| C7 | **CRITICAL** | Dashboard | PID stored is server, not task — stale-lock broken |
| C8 | **CRITICAL** | Analysis | LiquidationEstimator broken SMA — false positives |
| C9 | **CRITICAL** | Utils | `pipeline_utils.py` undefined `logger` — NameError crash |
| C10 | **CRITICAL** | Evolution | `evolver_sandbox.py` missing `symbol` arg — evolution broken |
| C11 | **CRITICAL** | Session | `run_session.py` `args.sampling_mode` AttributeError — crash |
| C12 | **CRITICAL** | Scripts | `calculate_qty.py` missing `sys.path` — standalone crash |
| H1 | **HIGH** | Analysis | VolumeProfile `iterrows()` on hot path |
| H2 | **HIGH** | Analysis | Kline processing done 2× per observation |
| H3 | **HIGH** | Analysis | ATR scaling uses linear instead of √time |
| H4 | **HIGH** | Math | Division-by-zero risk in `project_holding_time` |
| H5 | **HIGH** | AI | OpenAI client has no default timeout |
| H6 | **HIGH** | Margin | Config re-read from disk on every order operation |
| H7 | **HIGH** | Utils | `CongestionController` not thread-safe |
| H8 | **HIGH** | Sniper | State locks consumed for non-firing signal types |
| H9 | **HIGH** | Config | `RegimePhysicsConfig` constructed in two places |
| H10 | **HIGH** | Guardian | Time-stop uses stale holding-time projection |
| H11 | **HIGH** | Margin | Brittle base-asset extraction in `get_symbol_position` |
| H12 | **HIGH** | Dashboard | Backtest session filename never captured |
| H13 | **HIGH** | Debate | Fast-pass can skip audits after market regime change |
| H14 | **HIGH** | Exchange | Pagination returns partial data silently |
| H15 | **HIGH** | Risk | BTC equity conversion affects non-BTC position sizing |
| H16 | **HIGH** | Order Exec | `current_price` may be None in pivot — TypeError |
| H17 | **HIGH** | Order Exec | Unsafe `get_symbol_position()` in trailing stop — AttributeError |
| H18 | **HIGH** | Guardian | Zero `projected_holding_hours` triggers immediate time-stop |
| H19 | **HIGH** | Order Exec | No NEUTRAL direction guard in `sync_with_opinion` |
| H20 | **HIGH** | Base Agent | Simulated tool calls silently dropped when "opinion" present |
| H21 | **HIGH** | Debate | Math fact check executed BEFORE type validation |
| H22 | **HIGH** | Debate | Amnesty clause too broad — bypasses all subsequent rounds |
| H23 | **HIGH** | Fitness | Config defaults out of sync with YAML (60 vs 70, 15 vs 5) |
| H24 | **HIGH** | Sniper | Redundant config disk reads in run loop |
| M1-M20 | **MEDIUM** | Various | Regime defaults, audit bias, FP equality, auth, XSS, Calmar ratio, etc. |
| L1-L18 | **LOW** | Various | Edge cases, documentation, test coverage, magic numbers |
| P1-P16 | **PROMPT** | Strategy | LLM time calc always overwritten, visual context unused (DeepSeek no-vision), stale benchmarks |
| CFG1-CFG14 | **CONFIG** | YAML | DeepSeek no-vision gap, aggressive trailing, tight slip buffers, fitness defaults mismatch |

---

## Recommended Action Priority

### Immediate (Today/Tomorrow) — Runtime Crash Bugs
1. **Fix C9** — Add `import logging; logger = logging.getLogger(__name__)` to `pipeline_utils.py` (one-line fix)
2. **Fix C10** — Add `symbol=symbol` to `BinaryStarOrchestrator(...)` in `evolver_sandbox.py:124` (one-line fix)
3. **Fix C11** — Remove `{args.sampling_mode}` from log line in `run_session.py:278` (one-line fix)
4. **Fix C12** — Add `sys.path.insert(0, PROJECT_ROOT)` to `scripts/calculate_qty.py` (two-line fix)
5. **Fix C1** — Fix `backtest.py:182` import path `src.infrastructure.exchange.binance.client` → `src.infrastructure.binance.client` (one-line fix)

### Short-Term (This Week) — Data Correctness & Safety
6. **Fix C3** — CVD calculation formula: use `taker_buy_quote`/`quote_volume` ratio
7. **Fix C6** — `elif` → `if` in `GeminiAdapter` for `response_mime_type`
8. **Fix C5** — Conditional `sideEffectType` based on order side (BUY→MARGIN_BUY, SELL→AUTO_REPAY)
9. **Fix C8** — SMA calculation: NaN-prefix first `period-1` elements
10. **Fix C7** — Replace `os.getpid()` with unique `run_id` in session_run/backtest status files
11. **Fix H3** — ATR timeframe scaling from linear to `sqrt(ratio)`
12. **Fix H16** — Add `None`/zero guard before `current_price <= entry_price` comparison
13. **Fix H17** — Extract `pos` variable with None guard in `_migrate_trailing_stop`
14. **Fix H18** — Guard against zero `projected_holding_hours` in time-stop check
15. **Fix H19** — Add NEUTRAL guard at top of `sync_with_opinion`
16. **Fix H20** — Extract simulated tool calls even when "opinion" is present
17. **Fix H23** — Align `fitness_evaluator.py` class defaults with `global_config.yaml`

### Short-Term (This Sprint) — Strategy & Architecture
18. **Fix C2** — Either implement liquidation endpoint or remove liquidation-based triggers from Sniper
19. **Fix C4** — Place new OCO BEFORE canceling old in pivot logic
20. **Fix H5** — Add default timeout to OpenAI client constructor
21. **Fix H8** — Move state lock acquisition after hit selection in `SniperTrigger.evaluate()`
22. **Fix H11** — Replace `symbol.replace("USDT", "")` with proper base/quote parsing
23. **Fix H6** — Cache config in `MarginOrderExecutor.__init__` (stop per-call YAML reads)
24. **Fix H21** — Move type validation (isinstance check) before math checker call
25. **Fix H22** — Add counter or market-change check to amnesty clause fast-pass
26. **Fix H24** — Remove redundant `load_global_config()` calls, use `self.global_cfg`
27. **Add H4 guard** — `effective_velocity` minimum threshold in `project_holding_time`
28. **Review CFG6** — Either switch to vision-capable provider or remove `[VISUAL_CONTEXT]` references from prompts

### Medium-Term (Backlog)
29. **H1/H2** — Vectorize volume profile `iterrows()` + deduplicate kline processing
30. **M1** — Distinguish "sensor failure" vs "normal market" in regime defaults (use sentinel)
31. **M10** — Enforce dashboard authentication on write endpoints
32. **M11** — Sanitize dashboard `innerHTML` assignments
33. **Add** account-level drawdown circuit breaker (currently absent despite strategy intent)
34. **Add** startup config validation for all ~150 critical parameters
35. **Add** test coverage for SniperScout, SniperTrigger, Guardian, MarketObserver end-to-end
36. **Implement** degraded mode for AI provider outages (fallback to deterministic strategy)
37. **P5** — Clarify Confidence Calculus max-80-points-during-planning in session.md
38. **P10** — Sync visual context instructions with actual provider capability (vision vs text-only)

---

## Test Suite Assessment

All **150 tests pass** in 5.89 seconds. Coverage is good for the math/config layers. Key gaps:
- **No tests for SniperScout, SniperTrigger, Guardian** (the real-time trading heart)
- **No tests for MarketObserver end-to-end** (the data pipeline entry point)
- **No CLI integration tests** for any `run.py` subcommand
- **No tests for DebateLoop fast-pass** (complex deterministic bypass logic)
- **No tests for `clean_neutral_sessions.py`** or **`calculate_qty.py` main()**

---

*Generated by Claude Code on 2026-06-24. Full scope: 65 source files, 4 prompt templates, 3 config YAML files, ~12,000 lines of Python, 150 tests, 6 parallel subagent reviewers.*
